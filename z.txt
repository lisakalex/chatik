1. save a chatgpt page as webpage complete and name it index.html
2. copy index.html and the index_files dir to noscript
3. go to noscript.py give a title (line 99) and run
4. create a dir if not there and subdir and copy index_files and index.html
5. go to index.html in root add the links

# start web server to run locally
cd ~/IdeaProjects/chatik && python3 -m http.server 8003

commit and it will be at
https://lisakalex.github.io/chatik/

~~~
Install uv
curl -LsSf https://astral.sh/uv/install.sh | sh
Then restart your terminal

cd ~/IdeaProjects/chatik
uv venv --python 3.13 .venv
source .venv/bin/activate
uv pip install -r requirements.txt
uv pip install package
uv pip install --upgrade package
uv pip install package==2.6.0
uv pip uninstall package
uv pip freeze > requirements.txt
deactivate

standard venv + pip (classic & compatible)
python3 -m venv ~/.venv
source ~/.venv/bin/activate
python -m pip install package
python -m pip install --upgrade package
python -m pip install package==2.6.0
python -m pip uninstall package
pip freeze > requirements.txt
python -m pip install -r /var/www/requirements.txt
deactivate

~~~
